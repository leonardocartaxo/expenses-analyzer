# Stack decision

**Status:** deferred (bootstrap 2 incomplete)

**Locked so far:**
- Full rewrite (not a revive of the prior Go codebase)
- Monorepo with backend + frontend (optional shared contracts package)

**Still open:** languages, frameworks, database, package manager / workspace tooling.

**Environment:** Dev Container is in place (`.devcontainer/`). Docker Compose / service images wait until the stack is locked.

Record the final choice here when bootstrap 2 completes, then reflect it in the first `/speckit-plan`.
